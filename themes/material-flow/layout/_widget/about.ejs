<section class="m_widget about">
    <% if(config.avatar) { %>
        <img class="avatar waves-image" src="<%= url_for(config.avatar) %>" alt="<%= config.avatar %>"/>
    <%} %>
    <div class="header fa fa-user">
        <%=config.author %>
    </div>
    <div class="content">
        <div class="desc">
            <%= config.description %>
        </div>
    </div>
</section>